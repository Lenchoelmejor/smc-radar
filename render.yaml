services:
  - type: web
    name: smc-radar
    runtime: python

    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10

    buildCommand: pip install -r requirements.txt
    startCommand: python main.py

    plan: free
    autoDeploy: true
    Fix Python version
